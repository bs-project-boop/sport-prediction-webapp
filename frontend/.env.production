# API base URL — use relative path so Caddy reverse proxy handles routing.
# When accessed via LAN IP directly, Vite substitutes this at build time;
# the App.tsx fallback `?? '/api'` handles the default case.
VITE_API_BASE_URL=
